<%= render partial: 'users/user', object: @user %>
<% @posts.each do |post| %>
    <%= render partial: 'posts/post', object: post %>
    <div class="comments">
      <%= render partial: 'shared/comment', collection: post.recent_comments %>
    </div>
<% end %>